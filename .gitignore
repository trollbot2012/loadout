task_plan.md
findings.md
progress.md
GATES.md
__pycache__/
.pytest_cache/
.claude/settings.local.json
# Machine-local: generated from the skill bodies installed on one machine, so it both
# describes an inventory that is not portable and would publish it. Generate per install.
references/skill-notes.md
# Machine-local for the same reason, and more so: its records name this machine's absolute
# skill paths and the digests of the bodies installed here.
references/skill-assessments.json
# devteam pipeline run state: machine- and run-local, like the planning files above
.planning/
